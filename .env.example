# Copia questo file in ".env" (stesso nome, senza .example) e incolla la tua API key.
# La trovi su https://console.anthropic.com
# IMPORTANTE: il file .env NON viene caricato su GitHub (vedi .gitignore). La key resta sul tuo PC.

ANTHROPIC_API_KEY=sk-ant-incolla-qui-la-tua-key
